"use strict";

//jquery
$(function() { 

    //form validation 
    $( 'form' ).validate();
    
});

/*
 $(this).attr( 'disabled', true );

        var name = $( '#name' ).val().length; 
        var email = $( '#email' ).val().length; 
        var disabled = $(this).attr( 'disabled', true );
        var on = $(this).attr( 'disabled', false );

        disabled; 

       if ( name > 0 && email > 0 ) {
             on;
       } else {
           disabled; 
       }

    }); 
*/ 